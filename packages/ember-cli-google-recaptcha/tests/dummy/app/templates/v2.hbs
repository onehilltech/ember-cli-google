{{g-recaptcha-v2 verified=(action (mut response)) expired=(action "expired") reset=reset}}

{{#if response}}
  <p>Response: {{response}}</p>

  <a href="#" {{action (mut reset) true}}>Reset</a>
{{/if}}
