<p><LinkTo @route="index">Back to index</LinkTo></p>

<GRecaptchaInvisible @verified={{action (mut response)}} @expired={{action (mut expired) true}} @reset={{reset}} @execute={{reset}} />

<p>Response: {{response}}</p>

{{#if expired}}
  The response has expired. <a href="#" {{action "reset"}}>Reset</a>
{{/if}}
